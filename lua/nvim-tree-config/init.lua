require'nvim-tree'.setup {
  auto_close = true,
  view = {
    width = 30,
  },
  git = {
    enable = true,
  },
  update_focused_file = {
    enable = true,
    update_cwd = true,
  },
  gitignore = 1,
}
